apiVersion: v1
kind: Service
metadata:
  name: core-frontend-reader
  namespace: core-frontend
spec:
  type: ClusterIP
  selector:
    app: core-frontend-reader
  ports:
    - name: http
      port: 80
      targetPort: http